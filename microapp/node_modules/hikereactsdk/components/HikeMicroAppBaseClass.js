import React from 'react';
import { NativeModules, Platform} from 'react-native';
const HikeUtilsModule = NativeModules.HikeUtilsModule;
import Logger from '../logger/Logger';
export default class HikeMicroAppBaseClass extends React.Component {
  constructor(props) {
		super(props);

	}
  componentDidMount() {
    Logger.debug("Base Class Component did Mount Called");
    try {
      if (Platform.OS === "android"){
        var initData = {};
        Logger.debug("Initialising");
        HikeUtilsModule.init(initData);
      } else{
        //TODO : Wait for iOS
      }
      
    } catch (e) {
      Logger.debug("iniy method not available");

    } finally {

    }

	}
}
